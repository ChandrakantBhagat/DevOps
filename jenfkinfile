pipeline {
    environment {
        DYNATRACE_API_TOKEN = credentials('') // Store token in Jenkins credentials
        DYNATRACE_BASE_URL = 'https://kzo05756.live.dynatrace.com'
    }
    stages {
        stage('Deploy with Monaco') {
            steps {
                sh 'monaco deploy -e environments.yaml'
            }
        }
    }
}
